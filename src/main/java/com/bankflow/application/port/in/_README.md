# application/port/in

Interfaces dos casos de uso (ex: `RealizarTransferenciaUseCase`, `AbrirContaUseCase`).
São o "contrato de entrada" da aplicação — implementadas pelas classes em
`application/usecase` e chamadas pelos adapters de entrada (controllers).
