# application/usecase

Implementações dos casos de uso (ex: `RealizarTransferenciaService`). Orquestram
o domínio + as portas de saída, mas não contêm regra de negócio em si — a regra
mora nas entidades de `domain/model`.
